
extends layout

block prepend title
  | #{ page.title + ' - '}

block content
      h4.post-title= page.title
        p.post-meta= moment.utc(page.date).format('DD. MMMM YYYY')
        p
          section.content!= typogr(page.html).typogrify()
