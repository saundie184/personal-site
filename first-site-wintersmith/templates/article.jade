
extends layout

block prepend title
  | #{ page.title + ' - '}

block content
      h4.post-title= page.title
        p.post-meta= page.date
        p
          section.content!= typogr(page.html).typogrify()
